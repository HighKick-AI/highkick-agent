# highkick-agent

## Build

```sh
python3 -m venv venv
source ./venv/bin/activate

poetry install --no-root
```

## Run

```sh
export CONFIG_PATH="/path/to/example-config.yaml"
```

```sh
make run-python
```
